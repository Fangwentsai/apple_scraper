# 核心套件 - 確保相容性
flask==2.3.3
requests==2.31.0
gunicorn==20.1.0
python-dotenv==1.0.0

# Line Bot (可選)
line-bot-sdk==3.5.0

# 排程器
schedule==1.2.0

# Firebase (可選)
firebase-admin==6.2.0
google-cloud-firestore==2.11.1

# 爬蟲 (如果需要)
# playwright==1.40.0  # 註解掉，因為可能有相容性問題 